export const routes = {
    root: "/CCC",
    base: "/"
}

export const DASHBOARD_PATH = `${routes.root}/dashboard`;
export const SESSION_TIMEOUT = `${routes.root}/session-timeout`;
export const ORG_DETAIL_PATH = `${routes.root}/org-detail`;
export const RESULT_PATH = `${routes.root}/search-results`;
export const GRP_DETAIL_PATH = `${routes.root}/groups`;
export const CONTACT_PATH = `${routes.root}/contacts`;
export const ADMIN_PAGE_PATH = `${routes.root}/admin`;
export const TASKS_DASHBOARD_PATH = `${routes.root}/tasks-dashboard`;
